root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build -o ./tmp/tuskira ."
  bin = "./tmp/tuskira"
  include_ext = ["go", "toml"]
  exclude_dir = ["tmp", "web"]
  delay = 1000

[misc]
  clean_on_exit = true
